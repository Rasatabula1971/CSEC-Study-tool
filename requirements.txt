fastapi>=0.111
uvicorn[standard]>=0.29
httpx>=0.27
sqlite-vec>=0.1.6
pydantic>=2.7
python-dotenv>=1.0
python-multipart>=0.0.9   # FastAPI form handling
pymupdf>=1.24           # PDF text extraction (ingest.py + extract.py, as fitz)
python-docx>=1.1        # notes-upload .docx text extraction; ingest_v2 MoE SLMS .docx
python-pptx>=0.6.23     # ingest_v2 MoE SLMS .pptx/.pptm slide-text extraction (PHASE: build)
python-frontmatter>=1.0 # ingest_v2 Caribbean AI .md YAML front-matter; brings PyYAML for manifests/MCQ maps
pytesseract>=0.3.10     # notes-upload image OCR (requires Tesseract binary)
Pillow>=10.0            # image loading for OCR
google-generativeai>=0.8 # optional cloud LLM for grading + classification (gemini_client.py)
anthropic>=0.39.0       # build-time lesson composition (anthropic_client.py, PHASE: build)
openpyxl>=3.1
pytest>=8.2
